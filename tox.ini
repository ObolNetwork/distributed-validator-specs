[tox]
env_list =
    all-checks
    pytest
    docs-build
skip_missing_interpreters = true

[testenv]
basepython = python3
uv_python = >=3.12

[testenv:all-checks]
description = Run all quality checks (lint, typecheck, spellcheck)
extras =
    lint
    typecheck
    test
    docs
commands =
    {[testenv:lint]commands}
    {[testenv:typecheck]commands}
    {[testenv:spellcheck]commands}

[testenv:lint]
description = Lint and code formatting checks (ruff)
extras = lint
commands =
    ruff check --no-fix --show-fixes src tests scripts
    ruff format --check src tests scripts

[testenv:fix]
description = Auto-fix linting and formatting issues (ruff)
extras = lint
commands =
    ruff check --fix src tests scripts
    ruff format src tests scripts

[testenv:typecheck]
description = Run type checking (mypy)
extras =
    typecheck
    test
commands = mypy src tests scripts

[testenv:spellcheck]
description = Run spell checking (codespell)
extras = docs
commands = codespell src tests scripts docs test_vectors consumers proto charon_anchor.json README.md CLAUDE.md --skip="*.lock,*.svg,.git,__pycache__,.mypy_cache,.pytest_cache" --ignore-words=.codespell-ignore-words.txt

[testenv:pytest]
description = Run tests (pytest)
extras = test
commands = pytest {posargs} tests/

[testenv:pytest-cov]
description = Run tests with coverage (pytest)
extras = test
commands = pytest --cov --cov-report=html --cov-report=term {posargs}

[testenv:docs]
description = Serve documentation locally (mkdocs)
extras = docs
commands = mkdocs serve {posargs}

[testenv:docs-build]
description = Build documentation (mkdocs)
extras = docs
commands = mkdocs build --strict {posargs}
